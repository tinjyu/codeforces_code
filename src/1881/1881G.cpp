#include <iostream>
using namespace std;
long long int n, m;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        cin >> n >> m;
        string s;
        cin >> s;
        s = " " + s;
        for (int i = 1; i <= n; i++)
        {
        }
    }
}